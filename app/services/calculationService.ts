import { fork } from 'child_process';

// const childProcessCalculation = (number: number) => {
//   const compute = fork('compute.js', [number.toString()]);
//   compute.send('start');
//   compute.on('message', sum => {
//     res.end(`Sum is ${sum}`);
//   });
// };

export {
  // childProcessCalculation
};
